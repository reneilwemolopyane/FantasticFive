# FantasticFive

I used the session in my navbar so that the agency name is fetched once during login and it can be displayed consistently across the navbar without needing to query the database on every page load.And i included the agency_header in all files ill be using.